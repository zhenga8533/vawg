import { Text } from "@chakra-ui/react";
import GameDetailGrid from "../../components/main/game/GameDetailGrid";

const GameMedia = () => {
  return <GameDetailGrid left={<Text>Hello</Text>} />;
};

export default GameMedia;
